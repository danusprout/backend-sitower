name: Deploy Production — Backend

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger production deploy via webhook
        run: |
          curl -s -o /dev/null -w "%{http_code}" \
            https://api.spektra.biz.id/webhook/deploy-backend
